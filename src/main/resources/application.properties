spring.profiles.active=local
spring.application.name=jb-backend

# JPA / Hibernate 설정 (개발 환경 최적화)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true